spring:
  cloud:
    config:
      name: gitconfigclient
      label: master
      discovery:
        enabled: true
        service-id: gitconfigserver
#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka/